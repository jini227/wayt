{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true
  },
  "exclude": [
    "node_modules",
    ".expo",
    "dist",
    "dist-web",
    "web-build",
    "ios",
    "android"
  ]
}
